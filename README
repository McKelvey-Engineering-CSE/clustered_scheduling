This branch adds a unified time module (see time_function.h) and a shared memory pointer module (see SharedMemoryPointer directory).
Be aware that the build of the clustering launcher of this branch didn't include the shared memory pointer, if you want to use it, simply remove the original barrier and create an instance of the shared memory pointer.
